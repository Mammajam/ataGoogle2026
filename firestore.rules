rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No client login. The Python agent uses Admin/ADC, which bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
