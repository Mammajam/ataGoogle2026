rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // No client login. The agent writes artifacts with the Cloud Storage SDK.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
